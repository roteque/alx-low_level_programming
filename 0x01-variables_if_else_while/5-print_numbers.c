#include <stdio.h>
#include <stdlib.h>
#include <math.h>
/**
 * main - A program that prints all lower case alphabet except q and e
 * Return: Always 0 (Succes)
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("\n%d", a);
	return (0);
}
