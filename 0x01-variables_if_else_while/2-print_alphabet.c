#include <stdio.h>
/**
 * main - A programe that prints lower case of aphabet
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c;
	for (c = 'a'; c <= 'z'; c++)
	{
	putchar(c);
	putchar('\n');
	}
return (0);

}
